{
  "name": "hablas-evo-cloudflare-infra",
  "private": true,
  "scripts": {
    "test": "node --test worker.test.mjs",
    "validate:wrangler": "npm test && wrangler deploy --dry-run --env=\"\" --config wrangler.jsonc && wrangler deploy --dry-run --env production --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
