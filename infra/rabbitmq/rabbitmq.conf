# Private network only; persistent node identity is declared in Compose.
listeners.tcp.default = 5672
loopback_users.guest = true
vm_memory_high_watermark.absolute = 256MiB
disk_free_limit.absolute = 2GB
# No topology/DLQ policy is invented here. Validate the application's declarations.
