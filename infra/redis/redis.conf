# Proposed for a NEW staging volume only; not a transition procedure for existing Redis.
bind 0.0.0.0
protected-mode yes
port 6379
dir /data
appendonly yes
appendfsync everysec
maxmemory 384mb
maxmemory-policy noeviction
# Authentication is required by compose.data.yml, never embedded in this file.
